<script setup></script>

<template>
  <main style="padding: 2rem; font-family: system-ui, sans-serif; max-width: 40rem">
    <h1 style="margin-top: 0">Grotix Web</h1>
    <p>
      La aplicación está montada con Vue Router. Si antes tenías dashboard, cultivos, contratos,
      etc., esas carpetas ya no están en <code>src</code>: hay que volver a traer el código desde
      Git (rama <code>develop</code> o la que usen).
    </p>
    <p>
      Comandos típicos:
      <code>git fetch origin && git checkout develop && git pull</code>
    </p>
  </main>
</template>
