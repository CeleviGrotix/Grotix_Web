# Desarrollo: copia a .env.development.local (no se sube a Git).
# Producción / Firebase: Vite lee .env.production (sí va en el repo) al hacer npm run build.
VITE_API_URL=https://tu-gateway.ejemplo.azurewebsites.net
